
import axios from 'axios';

const AxiosInstance = axios.create({
    baseURL: 'placeholder website' 
    /*swap with actual website when ready, finish looking at disclaimer*/
});

export default AxiosInstance;